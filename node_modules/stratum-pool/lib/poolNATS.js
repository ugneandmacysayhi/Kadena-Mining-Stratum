var NATS = require('nats');
/*
  let nc = NATS.connect({
     user:'client', 
     pass:'d@bXy89gher',
     json: true,
     dontRandomize: true,
     servers: [
          "nats://192.168.2.43:60100",
     ]}
  );
  nc.on('connect', (c) => {
     // Do something with the connection
      console.log("----- NATS connected -----");
  });
 
  nc.on('error', (err) => {
      console.log(err);
  });
*/
  var sendShare = function(shareData) {
//   	console.log("sendShare: " + JSON.stringify(shareData));
//	nc.publish("sharetest", shareData);
  }

module.exports = {sendShare};
